name: Test Custom Action

on: [push, workflow_dispatch]

jobs:
  run-custom-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run custom action
        uses: ppatel55/custom-action-runner@main
